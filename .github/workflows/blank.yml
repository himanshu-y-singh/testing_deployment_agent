# This workflow build and push a Docker container to Google Artifact Registry
# and deploy it on Cloud Run when a commit is pushed to the "main"
# branch.
#
# To configure this workflow:
#
# 1. Enable the following Google Cloud APIs:
#
#    - Artifact Registry (artifactregistry.googleapis.com)
#    - Cloud Run (run.googleapis.com)
#    - IAM Credentials API (iamcredentials.googleapis.com)
#
#    You can learn more about enabling APIs at
#    https://support.google.com/googleapi/answer/6158841.
#
# 2. Create and configure a Workload Identity Provider for GitHub:
#    https://github.com/google-github-actions/auth#preferred-direct-workload-identity-federation.
#
#    Depending on how you authenticate, you will need to grant an IAM principal
#    permissions on Google Cloud:
#
#    - Artifact Registry Administrator (roles/artifactregistry.admin)
#    - Cloud Run Developer (roles/run.developer)
#
#    You can learn more about setting IAM permissions at
#    https://cloud.google.com/iam/docs/manage-access-other-resources
#
# 3. Change the values in the "env" block to match your values.

name: 'Build and Deploy to Cloud Run'

on:
  push:
    branches:
      - '**'

env:
  PROJECT_ID: 'mimetic-retina-486715-i3' # TODO: update to your Google Cloud project ID
  REGION: 'us-central1' # TODO: update to your region
  SERVICE: 'test' # TODO: update to your service name
  WORKLOAD_IDENTITY_PROVIDER: 'projects/816163793182/locations/global/workloadIdentityPools/github-action/providers/github' # TODO: update to your workload identity provider

jobs:
  list-services:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      # ✅ FIX 1: Checkout (prevents workspace warning)
      - name: Checkout
        uses: actions/checkout@v4

      # ✅ FIX 2: Add project_id
      - id: auth
        name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: '${{ env.WORKLOAD_IDENTITY_PROVIDER }}'
          service_account: '${{ env.SERVICE_ACCOUNT }}'
          project_id: '${{ env.PROJECT_ID }}'

      # ✅ Setup gcloud CLI
      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2

      # ✅ List services
      - name: List Cloud Run Services
        run: |
          echo "Listing Cloud Run services..."
          gcloud run services list --region=$REGION --format="table(metadata.name, status.url)"
